name: Monoblocco real-browser rooftop trial
on:
  pull_request:
    branches: [main]
    paths:
      - 'dist/**'
      - 'tools/test-monoblocco-chromium.mjs'
      - '.github/workflows/hospital-monoblocco-browser.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  browser:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install temporary Playwright and Chromium
        run: |
          npm install --no-save playwright@1.55.1
          npx playwright install --with-deps chromium
      - name: Exercise actual WebGL roof and ride with keyboard
        run: |
          node tools/serve.mjs --host 127.0.0.1 --port 4173 &
          for i in $(seq 1 25); do if curl -fsS http://127.0.0.1:4173/ -o /dev/null; then break; fi; sleep 1; done
          node tools/test-monoblocco-chromium.mjs
      - name: Preserve screenshots and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: monoblocco-browser-evidence
          path: test-artifacts/
          if-no-files-found: ignore
